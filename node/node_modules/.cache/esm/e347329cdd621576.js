let express,bodyParser,path,fetch;_dd6‍.w("express",[["default",["express"],function(v){express=v}]]);_dd6‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_dd6‍.w("path",[["default",["path"],function(v){path=v}]]);_dd6‍.w("node-fetch",[["default",["fetch"],function(v){fetch=v}]]);require = require('esm')(module);




const app = express();
const port = 3000;

app.use(bodyParser.json());

// Menyajikan file CSS secara statis
app.use('/screen', express.static(path.join(__dirname, '..', 'screen', "720.css")));
app.use('/screen', express.static(path.join(__dirname, '..', 'screen', "1080.css")));
app.use(express.static(path.join(__dirname, 'portfolio')));
app.post('/webhook', (req, res) => {
  const webhookURL = 'https://discord.com/api/webhooks/1117494600605646938/co0LaW0tNL0yV5Kr_GxDq32XGabf7lVrw48-r3lbGTs-TaEBKxbrp2r47ycPm400VPNg';
  const perusahaanValue = req.body.perusahaan;
  const isiValue = req.body.isi;
  const payload = {
    content: `Pesan dari ${perusahaanValue}: ${isiValue}`
  };
  fetch(webhookURL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(payload)
  })
  .then(() => {
    console.log('Data berhasil dikirim ke webhook Discord');
    res.sendStatus(200);
  })
  .catch(error => {
    _dd6‍.g.console.error('Terjadi kesalahan:', error);
    res.sendStatus(500);
  });
});
app.get('/', (req, res) => {
  res.sendFile(path.join(__dirname, '..', 'index.html'));
});

app.listen(port, () => {
  console.log(`Server berjalan di http://localhost:${port}`);
});
